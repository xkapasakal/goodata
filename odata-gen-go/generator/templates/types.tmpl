package odata

import (
    "time"
    "github.com/nu7hatch/gouuid"
)

// Reference imports to suppress errors if they are not otherwise used.
var _ = gouuid.ReservedNCS


{{ range .EntityType }} 
type {{.Name}} struct { {{range .Property}} 
	{{.Name}} {{ .ConvertTypes }} {{end}} 
}
{{end}}
